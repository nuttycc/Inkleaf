# Project-wide Gradle settings (shared / CI-relevant).
# Machine-specific resource caps (jvmargs, kotlin.daemon.jvmargs, workers,
# priority) belong in the user Gradle home, not this file:
#   Windows: %USERPROFILE%\.gradle\gradle.properties
#   Unix:    ~/.gradle/gradle.properties
# CI overrides resources via ./gradlew -D... flags in workflow configs.
#
# IDE (e.g. Android Studio) settings can still override this file.
# https://www.gradle.org/docs/current/userguide/build_environment.html

# Reuse task outputs across builds and branch switches.
org.gradle.caching=true
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# When enabled, the Configuration Cache allows Gradle to skip the configuration
# phase entirely if nothing that affects the build configuration (such as build scripts)
# has changed. Additionally, Gradle applies performance optimizations to task execution.
org.gradle.configuration-cache=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
